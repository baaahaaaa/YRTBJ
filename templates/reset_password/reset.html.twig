
{% block title %}🔐 Réinitialiser votre mot de passe{% endblock %}

{% block body %}
<div class="password-reset-container">
    <div class="password-reset-card">
        <h2>🔑 Nouveau mot de passe</h2>
        <p class="info-text">
            Entrez votre nouveau mot de passe ci-dessous pour réinitialiser votre compte.
        </p>

        {{ form_start(resetForm, {'attr': {'class': 'reset-form'}}) }}
            <div class="form-group">
                {{ form_label(resetForm.plainPassword, 'Nouveau mot de passe', {'label_attr': {'class': 'form-label'}}) }}
                {{ form_widget(resetForm.plainPassword, {'attr': {'class': 'form-control', 'placeholder': 'Entrez votre nouveau mot de passe'}}) }}
                <small class="form-text text-muted">Votre mot de passe doit contenir au moins 8 caractères.</small>
            </div>

            <button class="btn btn-primary btn-block">🔄 Réinitialiser</button>
        {{ form_end(resetForm) }}

        <div class="text-center mt-3">
            <a href="{{ path('app_login') }}" class="back-link">🔙 Retour à la connexion</a>
        </div>
    </div>
</div>
{% endblock %}

{% block styles %}
<style>
    /* Conteneur principal */
    .password-reset-container {
        display: flex;
        justify-content: center;
        align-items: center;
        height: 80vh;
        background: linear-gradient(135deg, #007bff, #00d4ff);
    }

    /* Carte de réinitialisation */
    .password-reset-card {
        background: #fff;
        padding: 30px;
        border-radius: 10px;
        box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.2);
        max-width: 450px;
        text-align: center;
        animation: fadeIn 0.5s ease-in-out;
    }

    /* Titre */
    h2 {
        font-weight: bold;
        color: #333;
        margin-bottom: 20px;
    }

    /* Texte d'information */
    .info-text {
        font-size: 16px;
        color: #555;
        margin-bottom: 20px;
    }

    /* Champs de formulaire */
    .form-group {
        text-align: left;
        margin-bottom: 15px;
    }

    .form-label {
        font-weight: bold;
        color: #444;
    }

    .form-control {
        border-radius: 5px;
        padding: 10px;
        font-size: 16px;
    }

    .form-text {
        font-size: 12px;
    }

    /* Bouton d'envoi */
    .btn-primary {
        background-color: #007bff;
        color: white;
        padding: 12px;
        border-radius: 5px;
        text-transform: uppercase;
        font-weight: bold;
        transition: 0.3s;
    }

    .btn-primary:hover {
        background-color: #0056b3;
    }

    /* Lien de retour */
    .back-link {
        font-size: 14px;
        color: #007bff;
        text-decoration: none;
        font-weight: bold;
    }

    .back-link:hover {
        text-decoration: underline;
    }

    /* Animation */
    @keyframes fadeIn {
        from {
            opacity: 0;
            transform: translateY(-10px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }
</style>
{% endblock %}
