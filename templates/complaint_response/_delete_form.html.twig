<form method="post" action="{{ path('app_complaint_response_delete', {'id': complaint_response.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ complaint_response.id) }}">
    <button class="btn">Delete</button>
</form>
